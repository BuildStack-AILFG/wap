{
  "name": "whatsapp-automation",
  "private": true,
  "type": "module",
  "description": "Run the whole stack (Postgres, FastAPI backend, Next.js frontend) with `npm run dev`.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "kill": "node scripts/kill.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
